---
description: "Summarize the current conversation into a structured output (recap / meeting-notes / TIL / decision-log) without saving to disk."
argument-hint: "[mode]"
---

Invoke the `note-summary` skill from this plugin (`my-note-skills`).

Read the skill at `${CLAUDE_PLUGIN_ROOT}/skills/note-summary/SKILL.md` and follow its workflow exactly.

User arguments (optional mode — recap, meeting, til, decision): $ARGUMENTS
