---
description: "Find related documents in your Obsidian vault and update them with insights from the current conversation."
argument-hint: "[keyword|topic]"
---

Invoke the `note-update` skill from this plugin (`my-note-skills`).

Read the skill at `${CLAUDE_PLUGIN_ROOT}/skills/note-update/SKILL.md` and follow its workflow exactly. If `~/.claude/my-note-skills/config.json` does not exist, run the `note-setup` skill first.

User arguments (optional keyword or topic to focus on): $ARGUMENTS
