---
description: "Save the current conversation (or a specified file/topic) as a new note in your Obsidian vault Inbox."
argument-hint: "[file_path|topic]"
---

Invoke the `note-new` skill from this plugin (`my-note-skills`).

Read the skill at `${CLAUDE_PLUGIN_ROOT}/skills/note-new/SKILL.md` and follow its workflow exactly. If `~/.claude/my-note-skills/config.json` does not exist, run the `note-setup` skill first.

User arguments (optional file path or topic): $ARGUMENTS
