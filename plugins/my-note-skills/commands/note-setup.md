---
description: "Detect Obsidian vault structure and save my-note-skills config (~/.claude/my-note-skills/config.json)."
argument-hint: "[vault_path]"
---

Invoke the `note-setup` skill from this plugin (`my-note-skills`).

Read the skill at `${CLAUDE_PLUGIN_ROOT}/skills/note-setup/SKILL.md` and follow its workflow exactly.

User arguments (optional vault path): $ARGUMENTS
